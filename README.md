# Android-Boot-Image-V0-Opener
opens boot imaeg
it assumes you aren't an idiot so it has pretty much no error handling

# build?
```bash
gcc ./main.c -lm -o main
```

# run?
```bash
./main boot.img
```
